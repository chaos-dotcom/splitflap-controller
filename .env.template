# Template for environment variables used by the frontend (Vite).
# Copy this file to .env in the project root directory and fill in the values.
# Remember that backend variables go into a separate backend/.env file.

# --- Frontend Configuration (used by Vite build process) ---
# Variables exposed to the frontend MUST start with VITE_

# Base URL of your backend API server (where Express is running)
# Example: http://localhost:3001 or https://your-backend.example.com
VITE_API_BASE_URL=http://localhost:3001

# Optional OIDC Authentication Flag (Frontend)
# Set to true if OIDC is enabled on the backend, otherwise false or leave unset.
# This MUST match the OIDC_ENABLED setting in the backend/.env file for authentication to work correctly.
VITE_OIDC_ENABLED=false

# --- Display Configuration ---
# Number of characters in the split-flap display (default: 12)
VITE_SPLITFLAP_DISPLAY_LENGTH=12
