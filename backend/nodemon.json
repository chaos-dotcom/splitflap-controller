{
  "watch": ["src", "../src/types"], // Watch backend source and shared types
  "ext": "ts,json,yaml",            // Include yaml if scenes change
  "ignore": ["src/**/*.spec.ts", "dist"],
  "exec": "ts-node --project tsconfig.dev.json ./src/server.ts" // Use the dedicated dev config
}
